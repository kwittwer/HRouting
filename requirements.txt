# HRouting – Abhängigkeiten
# Installation: pip install -r requirements.txt

PySide6>=6.5
PyInstaller>=6.0
markdown>=3.5
jsonschema>=4.20

# MCP-Server (optional – App läuft auch ohne)
mcp[cli]>=1.0
uvicorn>=0.30
