[pytest]
testpaths = tests
addopts = --tb=short -q
timeout = 30
markers =
    gui: GUI tests require offscreen Qt
    unit: pure-Python unit tests, no Qt
    slow: known slow or flaky tests (skipped in fast CI runs)
